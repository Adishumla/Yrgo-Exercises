const calcArea = (x, y) => {
  return x * y;
};
const x = window.prompt("Enter the width of the rectangle:");
const y = window.prompt("Enter the height of the rectangle:");
console.log(calcArea(x, y));
